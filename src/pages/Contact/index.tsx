import React, { FC } from 'react';
import { IDefaultProps } from '../../engine/utils/common.types';
import { Text, View } from 'react-native';

const Contact: FC<IDefaultProps> = () => {
  return (
    <View>
        <Text>Contact</Text>
    </View>
  );
};

export default Contact;
